node {
  /* Requires the Docker Pipeline plugin to be installed */
  docker.image('blazemeter/taurus').inside("--entrypoint=''") {
    checkout scm
    stage('Test') {
      dir ('taurus') {
        sh '''
          ls -lat
          HOME=$PWD
          bzt --help
        '''
      }
    }
  }
}
