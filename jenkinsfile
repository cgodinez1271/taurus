node {
   stage('Build') {
      // Run the Taurus build
   }
   stage('Performance Tests') {
    // Get code from Github repository
    sh 'git clone git@github.com:cgodinez1271/taurus.git'
    parallel(
        BlazeMeterTest: {
            dir ('Taurus-Repo') {
                sh 'bzt <file_name>.yml -report'
            }
        },
        Analysis: {
            sleep 60
        })
   }
   stage(‘Deploy’) {
   }
}
